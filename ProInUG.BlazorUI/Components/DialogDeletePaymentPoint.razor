<MudDialog>
    <TitleContent>
        <MudText Typo="Typo.h6">
            <MudIcon Icon="@Icons.Material.Filled.DeleteForever" Class="mr-3 mb-n1" />
            @MudDialog?.Title
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudTextField Value="@PaymentPoint.Id.ToString()" Label="Payment point ID" ReadOnly="true" Class="mb-4"/>
        <MudTextField Value="@PaymentPoint.DeviceUri" Label="Device Url" ReadOnly="true" Class="mb-4"/>
        <MudTextField Value="@PaymentPoint.OperatorName" Label="Operator Name" ReadOnly="true" Class="mb-4"/>
        <MudTextField Value="@PaymentPoint.IsEnable" Label="Is Enable" ReadOnly="true" Class="mb-4"/>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Color="Color.Error" OnClick="DeletePaymentPoint">Delete Payment Point</MudButton>
    </DialogActions>
</MudDialog>